services:
  postgres:
    image: postgres:18.4
    container_name: otus-postgres-hw
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=otus-db-hw
